# This workflow uses actions that are not certified by GitHub.
# They are provided by a third-party and are governed by
# separate terms of service, privacy policy, and support
# documentation.
on: [push]

name: AzurePowerShellSample

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    
    - name: Login via Az module
      uses: azure/login@v1
      with:
        creds: ${{secrets.AZURE_CREDENTIALS}}
        enable-AzPSSession: true 
        
    - name: run seed
      run: |
        docker run -ti -e MONGODB_CONNECTION="mongodb://fabmedical-cdb-790341:XbdQREzDqZUT1qccQAy4IZdfrvYLriSzJktcocF2INByXIqQ2NAO4POoCpDSXG0730pGGzB9JJnhACDb4enSmQ==@fabmedical-cdb-790341.mongo.cosmos.azure.com:10255/?ssl=true&replicaS
et=globaldb&retrywrites=false&maxIdleTimeMS=120000&appName=@fabmedical-cdb-790341@" ghcr.io/kaminiwalunj/fabrikam-init:main

        azPSVersion: "latest"




                                                
